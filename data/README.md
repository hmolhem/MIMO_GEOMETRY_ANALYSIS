This directory is intended to hold processed datasets used by the analysis scripts.

Currently empty in the repository. Add data files here when needed. Large raw datasets should not be committed; prefer storing them externally and adding a small manifest here.

Suggested usage:
- Keep small sample datasets for smoke tests only.
- Add a README or manifest describing the data provenance.
