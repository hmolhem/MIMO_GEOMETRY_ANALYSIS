This folder contains canonical wrappers that forward to the project's authoritative core scripts located in `core/analysis_scripts/`.

The wrappers allow a single, stable path for CI and documentation while keeping the original implementation in `core/analysis_scripts` during migration.
